/// Corner radii from the design. Named so a screen never guesses a number.
abstract final class AppRadii {
  /// Standard list/content cards.
  static const card = 14.0;

  /// Larger cards, and the splash logo tile.
  static const cardLarge = 16.0;

  /// Hero and summary cards.
  static const hero = 18.0;

  /// Small icon chips.
  static const chipSmall = 10.0;

  /// Icon chips.
  static const chip = 12.0;

  /// Fully rounded pills.
  static const pill = 999.0;

  /// Bottom sheets — top corners only.
  static const sheet = 28.0;
}
